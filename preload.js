window.addEventListener('load', () => {
    /*The below code is when frame is disabled
    let element
    newFun = ()=>
    {
        setTimeout(()=>{},2000)
        let ele = document.createElement("div")
        ele.setAttribute("style","width: 100%;height:2%; -webkit-app-region: drag; position: absolute")
        console.log(element)
        element.insertBefore(ele,element.childNodes[0])
        console.log(element)
    }
    element = document.getElementById("app")
    newFun();*/

    /*var dragNdropWorkAround = setInterval(() =>{
        element = document.getElementsByClassName("app")
        console.log("hello world")
        console.log(element)
        if(element.length)
        {
            clearInterval(dragNdropWorkAround)
            newFun();
        }
    },2000)*/
})